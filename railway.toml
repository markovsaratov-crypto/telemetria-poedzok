[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[build]
buildCommand = "DATABASE_URL=file:./prisma/dev.db bun run db:generate && DATABASE_URL=file:./prisma/dev.db bun run build"
